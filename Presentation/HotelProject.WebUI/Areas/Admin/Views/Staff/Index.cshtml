@model IEnumerable<StaffViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Personeller</h4>
        <div class="table-responsive">
            <table class="table header-border table-hover verticle-middle">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Personel Adı</th>
                        <th scope="col">Unvanı</th>
                        <th scope="col" class="text-center">Düzenle</th>
                        <th scope="col" class="text-center">Sil</th>
                        <th scope="col" class="text-center">Detaylar</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        count++;
                        <tr>
                            <th>@count</th>
                            <td>@item.staffName</td>
                            <td>@item.staffTitle</td>
                            <td class="text-center"><a href="/Admin/Staff/UpdateStaff/@item.staffId" class="btn btn-rounded btn-outline-primary">Düzenle</a></td>
                            <td class="text-center"><a href="/Admin/Staff/DeleteStaff/@item.staffId" class="btn btn-rounded btn-outline-danger">Sil</a></td>
                            <td class="text-center"><a href="#" class="btn btn-rounded btn-outline-warning">Detaylar</a></td>
                        </tr>
                    }
                </tbody>
            </table>
            <a href="/Admin/Staff/AddStaff/" class="btn btn-rounded btn-outline-info">+ Yeni Personel Girişi</a>
        </div>
    </div>
</div>



